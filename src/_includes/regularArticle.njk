    <article>
      <img src="{{post.data.image}}" alt="{{title}}">
      <h3><a href="{{post.url}}">{{post.data.title}}</a></h3>
      <p>{{post.data.description}} <br> {{post.data.date | postDate}}</p>
      <a href="{{post.url}}" class="read-more">Read More →</a>
    </article>
